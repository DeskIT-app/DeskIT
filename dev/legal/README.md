# The owner's privacy-law papers (private)

DISTRIBUTION_PLAN.md 13.8: the six one-page documents the 2017 Data
Security Regulations ask of a database "managed by an individual". This
folder is excluded from `git archive` and the manifest (`dev/` is
export-ignored) — it never ships. None of these needs a lawyer to exist;
the one-hour review of D25 reads the privacy policy, the terms and the
tier reasoning together, after the first release.

Fill each file when the account service (chapter 8) is switched on;
until then they describe a database that does not exist yet.
