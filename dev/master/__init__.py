"""The owner's master app (dev only; `dev/` is export-ignore, so no
installed copy ever carries it). The spec is MASTER.md beside this file.

Six screens that READ, one verb that takes a row to a chat, one tick that
only his hand puts on a row. Nothing here writes into the app's stores.
"""
