# The base set: what every copy of DeskIT installs (DISTRIBUTION_PLAN.md
# 10.5, D19). `requirements.lock` is generated from this file with
#     pip-compile --generate-hashes --allow-unsafe --output-file requirements.lock requirements.txt
# and is the only file the build reads (10.3 steps 1 and 5); regenerate
# it whenever a line here changes. The owner's venv may hold more than
# this — the CUDA runtimes, skia, the odd tool — none of it ships.
sounddevice
numpy
pywin32
pycaw
comtypes                    # taskbar relaunch properties (dashboard.py), pycaw's own need
pillow                      # every card, the keyboard picture, the skin's textures

# Backend B: local Hebrew transcription (no quota, works offline). Brings
# ctranslate2, onnxruntime (Silero VAD), huggingface_hub, tokenizers and
# av along as its own dependencies.
faster-whisper

# pip as a plain package: the pack installer of chapter 6 runs
# `python -m pip` inside the embeddable tree, which ships no pip of its
# own (10.2). Hashed like everything else (--allow-unsafe lets pip-tools
# pin it).
pip

# NOT here, on purpose (10.5): nvidia-cublas-cu12 / nvidia-cudnn-cu12
# (1,986 MB — the GPU pack, pip-downloaded on demand with the NVIDIA EULA
# link, chapter 6); skia-python (the Skin pack — skin\__init__.py reports
# the skin off when the import fails and the Tk overlays take over); the
# `keyboard` PyPI package, which the local keyboard.py used to shadow —
# that module is keycaps.py now; google-genai (~50 MB with pydantic and
# google-auth), since Gemini speaks REST through net.py (gemini_pool.Client,
# plan 5.6). httpx still arrives as huggingface-hub's dependency; nothing of
# ours imports it.
